import React, { Fragment, useState } from "react";
import { Pipes } from "../components/Pipes";

export const PipesContainer = ({ distance }) => {
  const [map, setMap] = useState(null);

  return (
    <Fragment>
      {/* <Pipes map={map} /> */}
      {/* {pipes} */}
    </Fragment>
  )
}